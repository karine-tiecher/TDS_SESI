var crocs = 90;
var lapis = 1;
var chinelo = 10;

var ValorTotal = crocs + lapis + chinelo;
    let mensagem = ValorTotal > 100 ? ValorTotal - (ValorTotal* 0.10): ValorTotal;
    console.log("O valor do desconto é: " + mensagem);

